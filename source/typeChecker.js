const primitiveTypeChecker = {
  init(target) {

  },
  setProperty(target, config, name, newValue, errorReporter) {

  },

  arguments(target, config, name, arguments, errorReporter) {

  },

  returnValue(target, config, name, value, errorReporter) {

  },
};
